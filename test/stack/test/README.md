# Test

This is where the integration tests live

These are written in plain JavaScript as they're run on TypeScript, CoffeeScript, ES6 and ES5 versions of the stack. The
same test files are used for each language, just copied for each different stack language.
